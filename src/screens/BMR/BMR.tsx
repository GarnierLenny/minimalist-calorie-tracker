import React from 'react';
import { SafeAreaView, Text } from 'react-native';

const BMRScreen = () => {
  return (
    <SafeAreaView>
      <Text>BMR Screen</Text>
    </SafeAreaView>
  );
};

export default BMRScreen;
